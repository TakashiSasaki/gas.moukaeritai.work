# Data

`data/` contains **what was observed from external systems**.

Raw external observations are kept separate from the automation code that produced them. During Phase 1, Google Drive API inventory snapshots move to:

`data/inventory/drive-api/snapshots/`

Materialized Apps Script project state remains under `projects/`, and public GitHub Pages projections remain under `docs/`.
